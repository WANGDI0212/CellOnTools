% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CLsearchLabel.R
\name{CLsearchLabel}
\alias{CLsearchLabel}
\title{Search Cell Ontology by Label}
\usage{
CLsearchLabel(
  pattern,
  clData,
  ignore_case = TRUE,
  exact_match = FALSE,
  use_regex = FALSE,
  max_results = NULL
)
}
\arguments{
\item{pattern}{Character vector of search patterns.  NA and empty strings
are silently dropped.}

\item{clData}{An \code{ontology_index} object returned by \code{CLload()}.}

\item{ignore_case}{Logical; if \code{TRUE} (default), perform
case-insensitive matching.}

\item{exact_match}{Logical; if \code{TRUE}, require the full label to equal
the pattern (default: \code{FALSE}, partial/substring match).}

\item{use_regex}{Logical; if \code{TRUE}, treat \code{pattern} as a regular
expression; if \code{FALSE} (default), treat it as a literal string.
Ignored when \code{exact_match = TRUE}.}

\item{max_results}{Maximum number of results to return per pattern
(default: \code{NULL}, return all matches).  Must be a positive integer
if specified.}
}
\value{
Data frame with columns \code{pattern}, \code{id}, \code{label},
\code{match_type}.  Returns zero rows (with a warning) if no matches are
found.
}
\description{
Searches Cell Ontology term labels for one or more patterns and returns a
data frame of matching terms.

By default, patterns are treated as \strong{literal strings} (not regular
expressions).  Set \code{use_regex = TRUE} to enable regex matching.
}
\examples{
\dontrun{
clData <- CLload()

# Literal substring search (default)
CLsearchLabel("T cell", clData)

# Case-insensitive literal search
CLsearchLabel("lymph", clData, ignore_case = TRUE)

# Exact match
CLsearchLabel("T cell", clData, exact_match = TRUE)

# Regex search
CLsearchLabel("^CD[48]", clData, use_regex = TRUE, ignore_case = TRUE)

# Limit results per pattern
CLsearchLabel("cell", clData, max_results = 5)

# Multiple patterns
CLsearchLabel(c("T cell", "B cell", "macrophage"), clData, max_results = 5)
}
}
