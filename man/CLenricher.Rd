% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CLenricher.R
\name{CLenricher}
\alias{CLenricher}
\title{Cell Ontology Marker Gene Enrichment Analysis}
\usage{
CLenricher(
  gene,
  geneType = c("symbol", "entrezid"),
  species = c("human", "mouse"),
  pvalueCutoff = 0.05,
  pAdjustMethod = "BH",
  universe = NULL,
  minGSSize = 1L,
  maxGSSize = 10000L,
  qvalueCutoff = 0.2,
  readable = FALSE
)
}
\arguments{
\item{gene}{Character vector of gene symbols or Entrez IDs.}

\item{geneType}{Type of gene identifiers: \code{"symbol"} or \code{"entrezid"}.}

\item{species}{Species: \code{"human"} or \code{"mouse"}.}

\item{pvalueCutoff}{P-value cutoff (default: \code{0.05}). Must be between
0 and 1.}

\item{pAdjustMethod}{P-value adjustment method (default: \code{"BH"}).}

\item{universe}{Background gene set (default: \code{NULL}, use all genes in
the marker annotation).}

\item{minGSSize}{Minimum number of annotated marker genes a cell type must
have to be tested (default: \code{1}).}

\item{maxGSSize}{Maximum number of annotated marker genes a cell type may
have to be tested (default: \code{10000}).  Must be \code{>= minGSSize}.}

\item{qvalueCutoff}{Q-value cutoff (default: \code{0.2}). Must be between
0 and 1.}

\item{readable}{Logical; if \code{TRUE} and \code{geneType = "entrezid"},
convert Entrez IDs to gene symbols in the result (default: \code{FALSE}).}
}
\value{
An \code{enrichResult} object from \pkg{clusterProfiler}, or
\code{NULL} if no significant enrichment is found.
}
\description{
Tests a gene set for over-representation of Cell Ontology cell-type marker
genes using \code{clusterProfiler::enricher()}.
}
\examples{
\dontrun{
# Gene symbols
result <- CLenricher(c("CD3D", "CD3E", "CD8A", "CD8B"),
                     geneType = "symbol", species = "human")
library(enrichplot)
dotplot(result)

# Entrez IDs with readable output
result <- CLenricher(c("915", "916", "925", "926"),
                     geneType = "entrezid", species = "human", readable = TRUE)
}
}
