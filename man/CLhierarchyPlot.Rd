% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CLhierarchyPlot.R
\name{CLhierarchyPlot}
\alias{CLhierarchyPlot}
\title{Plot Cell Ontology Hierarchy}
\usage{
CLhierarchyPlot(
  ids,
  clData,
  include_ancestors = TRUE,
  max_hops = 3L,
  layout = c("sugiyama", "tree", "fr", "kk"),
  node_size = 3,
  label_size = 3,
  query_color = "red",
  ancestor_color = "lightblue",
  show_id = TRUE,
  id_label_sep = "\\n"
)
}
\arguments{
\item{ids}{Character vector of CL IDs to visualize.}

\item{clData}{An \code{ontology_index} object returned by \code{CLload()}.}

\item{include_ancestors}{Logical; if \code{TRUE} (default), include ancestors.}

\item{max_hops}{Maximum number of direct CL parent edges above each query
term (default: \code{3}). Passed to \code{\link{CLhierarchy}}; use
\code{NULL} to include all reachable CL ancestors.}

\item{layout}{Graph layout algorithm: \code{"sugiyama"} (default, recommended
for DAGs), \code{"tree"}, \code{"fr"}, or \code{"kk"}.}

\item{node_size}{Size of nodes (default: \code{3}).}

\item{label_size}{Size of text labels (default: \code{3}).}

\item{query_color}{Colour for query nodes (default: \code{"red"}).}

\item{ancestor_color}{Colour for ancestor nodes (default: \code{"lightblue"}).}

\item{show_id}{Logical; if \code{TRUE} (default), display both CL ID and
label; if \code{FALSE}, display label only.}

\item{id_label_sep}{Separator between ID and label when \code{show_id = TRUE}
(default: \code{"\n"}).}
}
\value{
A \code{ggplot} object, or \code{NULL} (invisibly) if the hierarchy
contains no edges (with a warning).
}
\description{
Visualizes the Cell Ontology subgraph returned by \code{\link{CLhierarchy}}
using \pkg{ggraph}.  Query terms are highlighted in a distinct colour.
}
\section{Distance convention}{

Ancestor inclusion uses graph-hop distance from each query term. The
\code{ancestor_count} node attribute remains a separate CL-only specificity
measure; see \code{\link{CLdepth}}.
}

\examples{
\dontrun{
clData <- CLload()
ids <- c("CL:0000084", "CL:0000236")

# Default plot
CLhierarchyPlot(ids, clData)

# Label only, custom colours
CLhierarchyPlot(ids, clData, show_id = FALSE,
                query_color = "darkred", ancestor_color = "steelblue")
}
}
